const userSchema = require('./userSchema');
const questionSchema = require('./questionSchema');
const questionListSchema = require('./questionListSchema');

module.exports = {
    userSchema,
    questionSchema,
    questionListSchema,
};